CREATE TABLE `payouts` (
	`id` text,
	`place` integer,
	`name` text,
	`country` text,
	`prizeTwd` integer,
	`buyInValue` integer,
	`eventId` text,
	`eventIndex` integer,
	`eventName` text,
	`flight` text
);
